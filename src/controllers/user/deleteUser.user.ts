import { NextFunction, Request, Response } from "express";

export const deleteUserHandler = async (
  req: Request,
  res: Response,
  next: NextFunction
): Promise<void> => {
    try {
        
    } catch (error) {
        
    }
};
